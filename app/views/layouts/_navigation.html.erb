<%# navigation styled for Zurb Foundation 5 %>
<nav class="top-bar" data-topbar>
  <ul class="title-area">
   
     <!-- Remove the class "menu-icon" to get rid of menu icon. Take out "Menu" to just have icon alone -->
    <li class="toggle-topbar menu-icon"><a href="#"><span>Menu</span></a></li>
  </ul>

  <section class="top-bar-section">
    <!-- Right Nav Section -->
    <ul class="right">
        <li><a  href="#" title="contact" data-reveal-id="contact_us">Contact</a></li>
    
          <% if current_user %>
            <li class="signed_in"><%= link_to "Signed in as #{current_user.username}", user_path(current_user) %></li>
            <li><%= link_to "Profile Settings", user_path(current_user) %></li>
            <li><%= link_to "Sign Out!", destroy_user_session_url, method: :delete %></li>
          <% else %>
            <li><%= link_to "Log In",  new_user_session_path %></li>
            
            <li><%= link_to "Sign Up!", new_user_registration_path,  id: "btn_signup" %></li>
            
          <% end %>
            
          </ul>
      </li>
    </ul>

    <!-- Left Nav Section -->
    <ul class="left">
      <li><%= link_to 'Home', root_url %></li>
    </ul>
  </section>
</nav>